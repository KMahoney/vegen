{
  "name": "vegen-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build-todo-base": "tsc && vite build --base /todo/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "vite": "^7.0.4"
  }
}
